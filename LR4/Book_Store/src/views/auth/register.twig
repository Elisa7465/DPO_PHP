<!DOCTYPE html>
<html>
<head>
    <title>Регистрация</title>
    <!-- Подключение Bootstrap CSS для удобной и красивой стилизации -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* Определение CSS-переменных для основных цветов */
        :root {
            --primary-color: #7FB77E;      /* Основной зелёный цвет */
            --primary-hover: #6ea76d;      /* Цвет при наведении */
            --background-color: #F7FFE5;   /* Светлый фон страницы */
            --card-color: #ffffff;         /* Цвет карточки (белый) */
            --text-color: #0A2A24;         /* Тёмный цвет текста */
        }

        /* Фон и цвет текста всей страницы */
        body {
            background-color: var(--background-color);
            color: var(--text-color);
        }

        /* Стили для карточки с формой регистрации */
        .card {
            border-color: var(--primary-color); /* Цвет рамки */
            box-shadow: 0 2px 10px rgba(127, 183, 126, 0.1); /* Тень */
        }

        /* Заголовок карточки */
        .card-header {
            background-color: var(--primary-color); /* Заливка */
            color: white;                           /* Белый текст */
            border-bottom: none;                    /* Без нижней границы */
        }

        /* Кнопка с основным цветом */
        .btn-primary {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }

        /* Эффект при наведении на кнопку */
        .btn-primary:hover {
            background-color: var(--primary-hover);
            border-color: var(--primary-hover);
        }

        /* Стили для поля ввода при фокусе */
        .form-control:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.25rem rgba(127, 183, 126, 0.25);
        }

        /* Стилизация ссылок зелёным цветом */
        a {
            color: var(--primary-color);
            text-decoration: none;
        }

        /* Ссылка меняет цвет при наведении */
        a:hover {
            color: var(--primary-hover);
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <!-- Заголовок формы регистрации -->
                    <div class="card-header">
                        <h3 class="text-center mb-0">Регистрация</h3>
                    </div>
                    <div class="card-body">
                        <!-- Если есть ошибка, выводим её в красном блоке -->
                        {% if error %}
                            <div class="alert alert-danger">{{ error }}</div>
                        {% endif %}
                        
                        <!-- Форма регистрации -->
                        <form method="POST" action="/register">
                            <div class="mb-3">
                                <label for="email" class="form-label">Email</label>
                                <!-- Поле для ввода email, обязательно для заполнения -->
                                <input type="email" class="form-control" id="email" name="email" required>
                            </div>
                            <div class="mb-3">
                                <label for="password" class="form-label">Пароль</label>
                                <!-- Поле для ввода пароля, обязательно -->
                                <input type="password" class="form-control" id="password" name="password" required>
                            </div>
                            <div class="d-grid">
                                <!-- Кнопка отправки формы -->
                                <button type="submit" class="btn btn-primary">Зарегистрироваться</button>
                            </div>
                        </form>
                        
                        <!-- Ссылка на страницу входа -->
                        <div class="text-center mt-3">
                            <a href="/login">Уже есть аккаунт? Войти</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
